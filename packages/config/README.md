# @utils-live/config

Shared configuration files for the utils.live monorepo.

## Exports

- **ESLint** (`eslint/index.js`) -- shared ESLint configuration for all packages
- **Prettier** (`prettier/index.js`) -- shared Prettier formatting rules
- **TypeScript** (`typescript/base.json`) -- base `tsconfig.json` that all packages extend
